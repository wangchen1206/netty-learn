syntax = "proto3";//版本
option java_outer_classname = "StudentPOJO";//生成的外部类名，同时也是文件名
//protobuf使用message管理数据
message Student {//会在StudentPOJO类中生成一个内部类Student，是真正发送的pojo对象
    int32 id = 1; //Student类中有一个属性名字是id,类型是int32（protobuf类型）,1表示属性序号，不是值
    string name = 2;
}